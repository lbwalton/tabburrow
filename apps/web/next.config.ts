import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Marketing site only for now (T24). Share pages (s/[slug]) and the
  // account area land in a later task once Supabase exists — keep this
  // config minimal until then.
};

export default nextConfig;
